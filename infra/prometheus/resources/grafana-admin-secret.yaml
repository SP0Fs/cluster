# Placeholder — overwrite via the bootstrap procedure in README.md
# before the first ArgoCD sync. The chart fails to render if
# `grafana-admin` Secret doesn't exist.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
type: Opaque
stringData:
  admin-password: REPLACE_ME_BEFORE_FIRST_SYNC
