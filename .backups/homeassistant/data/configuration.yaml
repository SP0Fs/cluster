
# Loads default set of integrations. Do not remove.
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 10.42.0.0/24

recorder:
  db_url: postgresql://spof:Uh2ahgaca^d6Pai8Ohvo@postgres-postgresql-primary.postgres.svc.cluster.local:5432/homeassistant_db
  purge_keep_days: 30
  exclude:
    domains:
      - automation
      - updater
    entity_globs:
      - sensor.weather_*
    entities:
      - sun.sun 
    event_types:
      - call_service

climate:
  - platform: climate_group
    name: 'Heizungen'
    temperature_unit: C  # default to celsius, 'C' or 'F'
    entities:
      - climate.heizung_flur
      - climate.heizung_schlafzimmer
      - climate.heizung_wohnzimmer
      - climate.heizung_bad
      - climate.heizung_arbeitszimmer
      - climate.heizung_kuche

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
